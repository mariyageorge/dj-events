import Layout from '../../components/Layout/Layout'

const AddNewEvent = () => {
  return (
    <Layout title="Add a new event">
      We are on the Add new event page
    </Layout>
  )
}

export default AddNewEvent
